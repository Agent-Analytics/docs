---
import SharedFooter from './generated/SharedFooter.astro';
const pathname = Astro.url.pathname;
const locale = pathname.startsWith('/he/') ? 'he' : pathname.startsWith('/zh/') ? 'zh' : 'en';
---

<SharedFooter locale={locale} />
